apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: quarkus-book-api-native
  namespace: dev
spec:
  template:
    spec:
      containers:
        - image: ivanfranchin/quarkus-book-api-native:1.0.0
          env:
            - name: MYSQL_HOST
              value: my-mysql
